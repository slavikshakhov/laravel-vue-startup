export default {
  state: {
    /*
        data1: {
            x: null,
            y: null
        }
        */
  },
  mutations: {
    /*
        setData1(state, payload) {
            state.data1 = payload;
        }
        */
  },
  actions: {
    /*
        setData1(context, payload) {
            context.commit("setData1", payload);            
        }
        */
    loadStoredState(context) {
      // ex. get data from localStorage and add parsed data to state via mutations
      // page refresh survival
    },
  },
}
